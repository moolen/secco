syntax = "proto3";

package agent;

service Agent {
    rpc RunTrace(RunTraceRequest) returns (stream RunTraceResponse) {}
}

message RunTraceRequest {
    string id = 1;
}

message RunTraceResponse {
    map<string, int64> syscalls = 3;
}
